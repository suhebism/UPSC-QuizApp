import React from 'react'

const Subjects = ({subjects}) => {
  return (
    <div>
      {/* {subjects.map((subject)=>{
        <div key={subject.name}>{subject.name}</div>
      })} */}
    </div>
  )
}

export default Subjects
